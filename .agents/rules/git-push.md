# Git Push Rule

After completing any feature update, bug fix, or code modification requested by the user, automatically stage (`git add`), commit with a clear message (`git commit`), and push changes to origin (`git push`).
